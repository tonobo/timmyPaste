{% haml %}

-extends "layout.haml"

-block main:
  %div.col-md-12
    %label for="key"
      {% trans %} key {% endtrans %}
    %input.form-control type="text" value="{{ key }}" name="key"
    %hr
    {% if raw %}
      %pre
        |{{ code }}
    {% else %}
        {{ code }}
    {% endif %}


{% endhaml %}
